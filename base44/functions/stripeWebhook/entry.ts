// Stripe removido — usando Mercado Pago. Esta função é mantida como stub (redeploy).
Deno.serve(async () => {
  return Response.json({ ok: true, message: 'Stripe desativado. Use Mercado Pago.' });
});